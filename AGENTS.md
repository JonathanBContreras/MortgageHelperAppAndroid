# Development workflow

Before new feature work, fetch origin and create a new feature branch from the
latest master. Preserve local user changes; use a separate worktree when needed.
Commit and push the feature branch for review. Await explicit user permission
before merging or pushing feature changes to master.

Run appropriate calculation and Android UI tests for mortgage behavior changes.
Keep estimate sources and assumptions documented in docs/ESTIMATES.md.
